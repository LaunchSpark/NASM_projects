; CSC 3410 Assignment 2 - Program 4 instructions
; - Prompt the user to enter a two-character string and store it in memory allocated in a single BSS directive (e.g., resb 3).
; - Swap the two character bytes in memory so their order is reversed.
; - Print the program title, the prompt, and after swapping, print the modified string.
; - Remember to include space for a newline so the output matches the example.
